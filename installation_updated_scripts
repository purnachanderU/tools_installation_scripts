docker installation 
--------------------------------------------------------
#!/bin/bash

# Update packages sonarqube passwd: Admin@1234567 jenkins admin 1234  (sonarqube  dockerhub passwd/github passwd = 809999@Hi199
#sonarqube Jenkins1  sqa_63408047529534e5e366a63e034a25e7fb228d9f 
sudo apt update -y

# Install required packages
sudo apt install ca-certificates curl gnupg lsb-release -y

# Create keyrings directory
sudo mkdir -p /etc/apt/keyrings

# Add Docker GPG key
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

# Add Docker repository
echo \
"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
https://download.docker.com/linux/ubuntu \
$(lsb_release -cs) stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Update package index
sudo apt update -y

# Install Docker
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y

# Enable Docker service
sudo systemctl enable docker

# Start Docker
sudo systemctl start docker

# Add current user to docker group
sudo usermod -aG docker $USER

# Verify Docker
docker --version


Trivay installation 
==============================
#!/bin/bash

# Update packages
sudo apt update -y

# Install dependencies
sudo apt install wget gnupg lsb-release apt-transport-https -y

# Create keyrings directory
sudo mkdir -p /etc/apt/keyrings

# Add Trivy GPG key
wget -qO - https://aquasecurity.github.io/trivy-repo/deb/public.key | \
gpg --dearmor | \
sudo tee /etc/apt/keyrings/trivy.gpg > /dev/null

# Add Trivy repository
echo "deb [signed-by=/etc/apt/keyrings/trivy.gpg] https://aquasecurity.github.io/trivy-repo/deb generic main" | \
sudo tee /etc/apt/sources.list.d/trivy.list

# Update packages
sudo apt update -y

# Install Trivy
sudo apt install trivy -y

# Verify installation
trivy --version


==========================

Jenkin installations 
#!/bin/bash

# Update packages
sudo apt update -y

# Install Java
sudo apt install openjdk-17-jdk wget -y

# Download Jenkins WAR file
wget https://get.jenkins.io/war-stable/latest/jenkins.war

# Create Jenkins user
sudo useradd -m -d /var/lib/jenkins -s /bin/bash jenkins

# Create Jenkins directories
sudo mkdir -p /var/log/jenkins
sudo mkdir -p /etc/jenkins

# Set permissions
sudo chown -R jenkins:jenkins /var/log/jenkins
sudo chown -R jenkins:jenkins /var/lib/jenkins

# Create systemd service file
sudo tee /etc/systemd/system/jenkins.service > /dev/null <<EOF
[Unit]
Description=Jenkins Service
After=network.target

[Service]
Type=simple
User=jenkins
ExecStart=/usr/bin/java -jar $(pwd)/jenkins.war --httpPort=8080
Restart=always

[Install]
WantedBy=multi-user.target
EOF

# Reload systemd
sudo systemctl daemon-reload

# Enable Jenkins
sudo systemctl enable jenkins

# Start Jenkins
sudo systemctl start jenkins

# Check Jenkins status
sudo systemctl status jenkins --no-pager

===============================================

